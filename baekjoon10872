#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdint.h>
#include <time.h>

int p(int n)
{
	if (n <= 1)
		return 1;
	else
		return n * p(n - 1);
}

int main()
{
	int n;
	scanf("%d", &n);
	printf("%d", p(n));
	return 0;
}
