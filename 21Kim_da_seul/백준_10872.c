#pragma warning (disable:4996)
#pragma warning (disable:6031)
#include <stdio.h>

int main()
{
	    int a;
	        int time = 1;
		    scanf(" %d", &a);
		        for (int i = 1; i <= a; i++)
				    {
					            time *= i;
						        }
			    printf("%d", time);
			        return 0;
}
